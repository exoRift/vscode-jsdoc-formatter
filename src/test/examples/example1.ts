/**
 * yoooo
 * @param arg1 longtest hello
 * @param longerarg2 short longhellothere
 * @returns some return
 */
function hello (arg1: string, longerarg2: number): void {
  /**
   * A nested function
   * @param arg Its argument
   * @prop arg.test *Its property
   */
  function nested (arg: object): void {

  }
}